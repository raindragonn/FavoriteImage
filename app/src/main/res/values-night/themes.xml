<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FavoriteImage" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/base</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:colorBackground">@color/background</item>
        <item name="android:textColorPrimary">@color/textColor</item>
        <item name="android:textColorHint">@color/textHintColor</item>

        <item name="colorPrimary">@color/primary_600</item>
    </style>
</resources>